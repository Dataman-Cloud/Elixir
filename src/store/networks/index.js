import actions from './action'
import mutations from './mutations'

const state = {
  networks: {
    networks: [],
    total: 0
  },
  network: {}
}

export default {
  state,
  actions,
  mutations
}
