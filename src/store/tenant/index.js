import actions from './action'
import mutations from './mutations'

const state = {
  tenants: {
    tenants: [],
    total: 0
  },
  tenant: {

  }
}

export default {
  state,
  actions,
  mutations
}
